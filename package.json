{
  "name": "atlas",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -b && pnpm -F web build",
    "typecheck:test": "tsc -p packages/core/test/tsconfig.json && tsc -p packages/core/bench/tsconfig.json && tsc -p packages/datasets/test/tsconfig.json && tsc -p packages/query/test/tsconfig.json && tsc -p packages/protocol/test/tsconfig.json && tsc -p packages/server/test/tsconfig.json && tsc -p packages/client/test/tsconfig.json",
    "test": "vitest run && pnpm -F web exec ng test --watch=false",
    "test:web": "pnpm -F web exec ng test --watch=false",
    "lint": "eslint .",
    "format": "prettier --check .",
    "format:fix": "prettier --write .",
    "e2e:web": "pnpm -F web e2e",
    "verify:dist": "node scripts/verify-dist.mjs"
  },
  "devDependencies": {
    "@types/node": "^24",
    "eslint": "^10.4.1",
    "prettier": "^3.8.4",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.61.0",
    "vitest": "^4.1.8",
    "yaml": "^2.9.0"
  }
}
